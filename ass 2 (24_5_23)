#include<stdio.h>
main()
{
	int a[10],se,i,n,c=0;
	printf("enter the range\n");
	scanf("%d",&n);
	for(i=0 ; i<n ; i++)
	{
		scanf("%d",&a[i]);
	}
	printf("The elements are");
	for(i=0 ; i<n ; i++)
	{
		printf("\n%d",a[i]);
	}
	printf("\nEnter the required element:");
	scanf("%d",&se);
	for(i=0 ; i<n ; i++)
	{
		if(se==a[i])
		{
			c++;
		}
	}
	printf("Frequency of the element is: %d",c);
}
